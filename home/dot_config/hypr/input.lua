-- Keep only your personal input overrides here. Uncommented settings below
-- replace Omarchy's defaults.

-- Map the right Super key to Menu.
--
-- kb_options replaces Omarchy's default wholesale rather than merging, so the
-- two options Omarchy sets by default are restated here alongside the override.
hl.config({
  input = {
    kb_options = "compose:caps,shift:both_capslock_cancel,altwin:menu_win",
  },
})
